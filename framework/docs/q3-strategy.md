# Q3 策略迭代

已实现（见 `strategy.py` 的 `Q3Strategy` / `Q3Params`）：

| 版本 | 内容 | mock 上的效果（seed 1-12） |
| --- | --- | --- |
| v0 | 7 点覆盖扫（圆心+六边形 1400m）+ 20 频道全扫 + 命中清除 | 清除率 1.000，平均约 586s |
| v1 | 每条示向度读数给出 ±1° 扇形，多扇形求交得到保守可行域 | — |
| v2 | 引入 `direction` 蕴含的 1500m 半径凸约束，把细长区域收成有界区域 | 区域从无界变有界 |
| v3 | 可行域最小包围圆半径 <= 20m 即瞄准圆心清除（确定性判据） | 清除失败率 0 |
| v4 | 逼近式精定位：远距三读数只能收到 ~30m，再逼近补读数 | 清除率 1.000 |
| v5 | 覆盖半径下界推导 `r >= 900√3 − 100√19`，默认半径 1400 -> 1200 | 平均 586s -> 557s |
| **v6** | **访问顺序：按频道号 -> 在线最近邻（每处理完一个源，用最新估计点重挑最近的下一个）** | **平均 557.9s -> 352.5s** |
| **v7** | **逼近半径 `(200,400,700)` -> `(30,60,120,250)`；打分加入"候选点到可行域平均距离"项** | **-> 344s** |
| **v8** | **`skip_localized_probes`：可行域已 <= 20m 的频道不再重复探测（一频道一源）** | **-> 344s（省 ~140 次检测中的 8 次）** |
| — | 合计（200 seed 验证） | **虚拟时间 7098 -> 4435s，路程 30666 -> 17609m，平均定位清除时间 558.4 -> 349.8s，清除率 1.000** |

## v6 为什么是最大的一步

拆解旧版的路程构成（`script/q3_time_audit.py`，24 seed）：

| 类别 | 路程 | 占比 |
| --- | --- | --- |
| `measure@refine` | 14653 m | 48.1% |
| `clear@localized` | 7562 m | 24.8% |
| `measure@cover-scan` | 7200 m | 23.7% |

而 `_finish_pending` 原来按 **频道号升序** 处理待清频道——等于让机器狗在 1800m 的圆域里
按频道号随机横跳。**"访问所有源"本身就是一个 TSP**，仅换成在线最近邻，路程就掉 37%。

同族距离下界（覆盖扫描 7200m + 扫描后访问全部源的最优路径 8977m）= **16177m**，
当前实现 17609m，**只差 8.8%** —— 这条策略族的路线已经基本走到头。

## 关键权衡（都用 `script/run_batch.py` 量过）

1. **先全扫再逐个定位**（`localize_before_full_scan=False`）比"边扫边定位"好：
   后者每解决一个频道就要折返扫描点，实测多走约 4km。
2. **路程权重不能太大**：`travel_weight > 1/1000` 会让精定位点为了省路程牺牲角度分离，
   清除率掉到 0.97 左右。默认 `1/1200`。
   注意这是**旧打分**（只有"分离度 − 路程"两项）的结论；v7 给打分补上了
   "离源多近"这一项之后，权重语义已经不同。
3. **扫描半径不是越小越好**：路程随半径单调下降，但覆盖余量随半径下降。
   1125m 已贴着理论下界（余量 0.9m），1200m 余量 31m，1400m 余量 86m。
   **默认 1200m**：1125 只省 2.6%，但"确保全部清除"是硬前提，不值得冒险。
4. **覆盖扫描设计已接近最优**：把"每停点要扫 20 个频道（120s）"算进去之后，
   在正 n 边形巡游族里穷举（`script/q3_cover_design.py`），同余量下最优只比
   当前设计省 17s。**不要在这里继续投入。**
   反例提醒：11 边形 r=870 的路程只要 5772m（vs 7200m），但 12 个停点要多花
   600s 检测时间——**路程最短 ≠ 时间最短**。

## 下一步（按预期收益排序）

1. **逼近段（7005m / 41%）**：把"一步贪心打分"升级为**有 cost-to-go 的序贯决策**。
   状态 = (可行域多边形, 当前位置)，动作 = 下一个检测点，单步代价 = 路程/5 + 6s，
   终止条件 = 包围圆半径 <= 20m。可用小宽度 beam search 近似。
   两点测向的解析最优解是"先沿方位线接近到约 0.3d、再横向约 0.15d"，
   这与现在"在估计点周围 30/60/120/250m 环上按打分挑点"是两套逻辑。
2. **每个停点的 20 频道全扫（840s / 19%）**：现在只跳过"已 <= 20m"的频道，
   可以试更激进的剪枝（需要保守的正确性论证）。
3. **问题4**：定向源覆盖角只有 ±90°，`no_signal` 不再等价于"距离太远"，
   可行域必须加入"不落在覆盖角内"的**非凸**约束；策略需要分支 / 多假设。
4. **正式测试前的复现**：用 `ReplayClient` 把线上演练的 `run.jsonl` 重放，
   确认本地逻辑与线上一致后再跑正式测试；
   线上演练全流程见 `script/run_practice_online.py` 与 [`../说明.md`](../说明.md)。

---

## v9（分支 `feat/knowledge-matrix`）：知识矩阵 + 覆盖证书

这一步换的是**知识表示**，不是参数：把"逐频道的读数列表"升级成
`channel × path` 的观测台账（即用户提出的 `dataType.txt` 形态），
并把"确保全部清除"从硬编码 7 点变成**在线可计算、可审计**的覆盖证书。

完整推导、数据结构设计与实测对比见 [`../../../docs/knowledge-matrix.md`](../../../docs/knowledge-matrix.md)。

| 版本 | 内容 | 效果 |
| --- | --- | --- |
| v9a | `knowledge.py`：知识矩阵（正例/负例/清除失败全部入账，可导出 `dataType.txt`） | 负例首次进入决策 |
| v9b | `coverage.py`：由负例在线夹逼有效接收半径 R̂；栅格化覆盖判据（格角包含 ⇒ 严格保守） | 证书替代硬编码 7 点 |
| v9c | 频道级探测剪枝：`d(stop, P_c) > R̂` 的检测必然是 `not_find`，直接跳过 | 检测次数 142.5 → 132 |
| v9d | 精化扫描：把"只有 1 个读数"的频道集中一趟补读数 | 省 n−1 趟折返 |
| v9e | 逼近段解析式化：走"弦"到 0.6d̂ 再读数；ρ≤60m 时改走"可行域最近点" | 清除点不再贴边界空转 |
| v9f | 问题4：锥盲区判据（`invisible_mask` / `hidden_mask`）+ 负例圆盘排除在定向场景关闭 | 清除率 0.870 → 0.922 |
| v9g | 收尾：重访最后一条有效示向度 + 契约布局完备性重扫 | 兜底"确保全部清除" |

实测（30 seed，同一批案例，`python _bench_ab.py --seeds 1-30 [--directional]`）：

| | 旧 `Q3Strategy` | 新 `KnowledgeSearchStrategy` |
| --- | --- | --- |
| 问题3 全清除 | 30/30 | 30/30 |
| 问题3 平均定位清除时间 | 332.19 s | **330.24 s** |
| 问题3 检测次数 | 142.5 | **132.0** |
| 问题4 全清除 / 清除比例 | 9/30 · 0.8704 | **11/30 · 0.9216** |
| 问题4 平均定位清除时间 | 494.93 s | **458.58 s** |

也就是说：**问题3 打成平手（时间持平、检测次数省 7%），问题4 明显更好**，
而完备性从"写死的 7 个点"变成了可以随时验证的证书。
