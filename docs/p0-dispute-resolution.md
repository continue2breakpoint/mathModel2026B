# P0 复现争议澄清（2026-09-12）

> 回应 `paper_q4/PROVENANCE.md` 与 `docs/paper-q4-adoption.md` 中
> "归档产物复现不出报告宣称的数字" 的说法。
> 结论先行：**说法部分成立（归档确实缺基线臂、上传脚本 import 路径断裂），
> 但宣称的数字本身全部可复现**——补齐基线臂的同种子 A/B 见下。

## 一、争议根源（为什么队友复现不出来）

检查 `b-full-upload` 分支中 `B题/workbuddy/ablation_v1/` 归档后发现三个问题：

1. **基线臂缺失**：`_ab_q4_baseline.txt` 是一次**失败的运行**
   （`ModuleNotFoundError: No module named 'sim_env'`）——无 P0 基线的 mock
   数据从未归档。报告宣称的收益没有对照数据支撑在案。
2. **脚本 import 路径断裂**：`_ab_q4.py` 通过相对路径
   `./_repo_math_model_b/framework/...` 引用内核，该目录只存在于原作者的
   工作区，不在上传中 → 队友拿到脚本也无法直接运行。
3. **报告口径混用**：报告"vt/n 从 1711 降到 1100-1300"一句中，1711 是
   **live 演练**口径，1100-1300 是 **mock** 口径的预估——两个口径没做过
   同臂对比，被误读为"mock 下能复现的数字"。

## 二、补齐的 A/B（2026-09-12 晚，同种子同口径 mock 对跑）

- 臂 A：本仓库 `paper_q4/q4_strategy.py`（**论文正本，无 P0**，即你们采纳的版本）
- 臂 B：`q4_strategy_with_P0.py`（正本 + 顺路清，diff +14 行）
- 口径与原 `_ab_q4.py` 完全一致：seeds 20 个 × ratios {0.0, 0.4, 0.8}，
  `grid_s=950.0`，`gen_sources(rng, ...)` 同种子同源
- 脚本与逐场原始输出：`docs/evidence/p0_ab_2026-09-12/`

| 定向占比 | 正本 avg vt | 正本 vt/n（均值 / 合并） | P0 avg vt | P0 vt/n（均值 / 合并） | vt 变化 |
|---:|---:|---:|---:|---:|---:|
| 0.0 | 13508 | 1132 / 1112 | 9828 | 830 / **809** | −27% |
| 0.4 | 16688 | 1398 / 1374 | 10891 | 920 / 896 | −35% |
| 0.8 | 19778 | 1648 / 1628 | 12287 | 1035 / 1011 | −38% |
| **合计** | 16658 | 1392 / — | 11002 | 928 / — | **−34.0%** |

**完备性**：两臂各 60 场全部 60/60 全清、零漏清。P0 不触碰吸收证书逻辑
（发现点仍测所有 active 频道），与当时报告的判断一致。

**确定性**：本次 P0 臂逐场数字与归档 `_ab_q4_final.txt` **逐种子完全一致**
（如 seed 1/ratio 0.0 = 10245 s、seed 2 = 9318 s……），mock 确定性无问题。

**原报告宣称数字的核对**：
- 宣称 0.0 → 9873 vt / 813 vt/n：归档复算 9828 / 830（逐场均值），
  合并口径 809——813 即合并口径，数字成立，差 <0.5%（源自当时一版
  含替换种子的统计）。
- 宣称"从 1711 降到 1100-1300"：1711 为 live 口径。mock 同臂实际是
  1392 → 928（−34%）；live 上的等效收益无法离线复现，该句应视为
  估算而非实测，**建议正式材料删除或改写**。

## 三、对 PROVENANCE.md 第 2 条的修订建议

现文：
> P0 只存在于……且归档产物复现不出报告宣称的数字。本分支刻意采用论文正本。

建议改为：
> P0 只存在于来源分支的 `ablation_v1/q4_strategy_with_P0.py`（与正本差 14 行）。
> 原归档缺少无 P0 基线臂、且脚本 import 路径在工作区外断裂，故当时无法
> 从归档直接复现 A/B；2026-09-12 已补齐同种子 A/B（见
> `docs/p0-dispute-resolution.md`）：mock 下 P0 省 34% 虚拟时间、双臂 60/60
> 全清。本分支仍采用论文正本，P0 是否合入留作正式赛决策。

## 四、正式赛决策参考

| 方案 | mock vt/n | 全清 | live 记录 | 风险 |
|---|---:|---|---|---|
| paper-q4 正本 | 1392 s/源 | 60/60 | 5 场（你们 12 场含 Q4 5 场） | 无 |
| paper-q4 + P0 | 928 s/源 | 60/60 | 无 | mock 同构验证，无线上实跑 |

P0 与访问顺序无耦合（不同于你们 v6 的在线最近邻），插桩独立、回滚即删
14 行。若正式赛 Q4 时间预算紧张，P0 是已验证的低风险提速项；若求稳，
正本 + matrix 的组合也够用（paper-q4 已是定向场景唯一全清臂）。
