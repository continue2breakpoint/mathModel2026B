# Q3 优化 2026-09-12 深夜会话：参考解口径修正 + v10~v13 负结果记录

> **⚠️ 2026-09-13 口径更正（重要，先读这段）**
>
> 本文原先把 `_floor_q3_g.py` 给出的 **≈183.7 s/源** 称作 Q3 的"上帝下界"，并据此
> 写出"185 恰好贴在上帝视角地板上""到 185 需要结构级改动"这类断言。
> **这个下界口径是错的。**
>
> 独立复核 `review_20260913/审阅结论.md` §7 指出：`_floor_q3_g.py` 内部用
> **NN / 2-opt / or-opt 等启发式**求解"上帝信息"（源位置已知）问题。带上帝信息的
> **启发式可行解**只是该离线问题的一个**可行参考解**，给出的是**上帝信息最优值的一个
> 上界**，而不是任何下界；对**在线**问题（源位置未知）更不是下界。
>
> 因此本文一律改称 **上帝信息参考解（183.7 s/源）**，并明确：
>
> 1. **不能用它断言"185 不可达"**——183.7 本身不是认证下界，两者之间不存在
>    "贴地板"的关系；原表里"185 恰好贴在上帝视角地板上"一句作废。
> 2. **不能用"与 183.7 的差距"证明在线策略次优**——差距只是与一个启发式参考解的
>    经验比较，不构成次优性证明。
> 3. 要得到**可认证下界**，必须另外求解**松弛问题**（例如去掉在线信息约束后的松弛，
>    或对覆盖数与路由分别给出组合下界）。本仓库**目前没有**这样的下界，
>    本仓库**目前没有**这样的下界，也不声称存在：要得到它必须另行求解松弛问题
>    （或对覆盖数与路由分别给出组合下界），**这一项尚未做**。
> 4. 同理，下文的 196.9 s/源（按 clear=5s 修正 ≈199.9）是**固定 7 停靠布局下的
>    参考解**，同样**不是**认证下界。
>
> 本文其余内容（v8 实测分解、v10~v13 负结果与全部实测数字）不受影响，仍然有效。
> 详见 `docs/review-fixes-2026-09-13.md`。

## 一、参考解口径修正（重要）

此前口径"下界 202.4、185 不可达"**是错的**（那个 202.4 从来不是下界），
错在把覆盖集固定为"中心+六边形@1123"。
下表给出的是**四套上帝信息参考解**（均为启发式可行解）的对比，**不代表下界**：

| 方案 | 口径 | s/源 中位 |
|---|---|---|
| C | 固定 7 点环 θ=0 | 199.4 |
| D | 源位置免费充当覆盖点 | 222.4（劣！absent 探针在 13.5 个点都要测）|
| F | C + θ 扫描 + 环点吸附源(300m) | 194.3 |
| **G** | **覆盖点位置完全自由(局部搜索) + 联合 TSP + absent×7 探针** | **≈183.7**（G 脚本实测 190.8，但 k_abs 贪心高估为 8.5，取 7 后 ≈183.7）——**上帝信息参考解** |

- 纯源 TSP 中位 8858m；联合 TSP（源∪自由覆盖点）中位 9562m。
- **183.7 是参考解、不是地板**：启发式可行解只能说明"上帝信息问题最优值 ≤ 183.7"，
  它是**上帝信息最优值的上界**；它既不是在线问题的下界，也不是上帝信息问题的下界。
  因此"185 贴地板""185 不可达"两句**均无依据**；把在线策略与该值之差当作
  "次优性证明"同样无效。
- 另一个关键事实：mock 的回合只在主动 `/exit` 时结束，指标就是 `vt/cleared`，
  无源频道认证是**策略自己的口径**，不是模拟器强制。

## 二、v8 实测分解（30 seeds，中位：vt=3395, dist=13017, meas=111, n=13）

| 项 | v8 | 参考解 G（上帝信息，启发式） | 差距 |
|---|---|---|---|
| 行程 | 13017m (2603s) | 9562m (1912s) | +691s |
| 探针+切换 | ~740s | ~450s | +290s |
| 合计 | 3395s | ~2380s | +66.8 s/源 |

> 说"差距"只是与**参考解**的经验差距（v8 的 3395s 与参考解 ~2380s 相除得 ≈66.8 s/源），
> 不是与下界的差距，也不能读成"次优 X s/源"的证明。

插桩（`_diag_clears.py`，10 seeds）：

- direct 尝试 129 次：**2 条读数 63 次命中 86%**；**1 条读数 66 次命中 3%**（沿方位角猜 ~800m，误差 ~230m）
- localized（逼近环收敛）73 次，误差仅 2.9m——精定位流程本身是好的
- 离线研究（`_study_est.py`）：**84% 的源有 ≥2 个扫描停点可见**；2 条 LSQ 命中 82.8%，3 条 73%（几何差时反而更差），4 条 100%

## 三、正结果（微小）

| 配置 | avg_med | 备注 |
|---|---|---|
| v8 基线（1+7gon@1110, spl2） | 267.19 | 原起点 |
| + 六边形 1+6gon@1130（7 停点） | 266.68 | 少 1 套 absent 探针 |
| + scan_probe_limit=3 | **265.72** | **当前最优，30/30 全清** |

## 四、负结果（全部 30 seeds，30/30 全清前提下）

| 方案 | 做法 | 结果 | 死因 |
|---|---|---|---|
| v10 侧向补测 | 单读数时先去"估计点垂偏 400m"补一条 60-90° 分离读数再清 | 302.26 (+36) | 命中率 43→78%、逼近环 73→28 次都对了，但侧向点丢掉逼近进度，绕路 400m×6.6/seed > 省下的环 |
| v11 延迟调度 | 1 条读数等扫描点全部走完才可清 | 307.64 (+41) | 顺路清除的合并价值 > 精定位浪费；延迟破坏 interleaving |
| v12 猜距调优 | 单读数猜距 800→950/1000/1100/1200 | 292→345 单调劣化 | 楔形质心（≈v8 行为）已优于任何固定距离；单读数源其实离停点更近，"独占区在边缘"的理论错了 |
| v13 顺路探针 | 任务移动路径经过单读数频道检测点 800m 内且分离角≥12° 就顺路停 6s 补读数 | 266.33 (−0.35) | 六边形任意两停点路径离第三停点 ≥977m，扫描移动永远不触发；只有清源移动能触发，而那时单读数频道多半已被调度走 |
| follow_plan=true | 批量 TSP 计划 | 301.97 | 计划被过期估计骗，在线重排更稳 |
| schedule_min_readings=2 | 凑够 2 条才调度 | 314.77 | 同 v11 |
| max_refine_rounds 3/4、two_opt_passes=20 | | 逐位不变 | 旋钮已饱和 |

**结论：v8 的结构是强局部最优。** 剩余约 60~70 s/源 的差距（相对上帝信息参考解）= 在线信息约束（不知道源位置）+ 估计/精定位绕路，对局部修补免疫。

## 五、若要继续逼近参考解，剩余方向（未验证）

> 注意：下面的"目标"是逼近**上帝信息参考解 183.7**，而不是逼近某个已证明的下界——
> 183.7 随时可能被更好的上帝信息可行解推翻，185 也不构成任何"地板"。

1. **"足够近即算到"的 TSP**：清除任务从"点"改成"估计点 15m 邻域"，路径规划允许擦边通过，省掉为对准估计点的最后一段折返。
2. **清源任务的代价模型**：调度打分里把"到估计点的距离"从欧氏改为"插入当前路线的边际成本"（现在 travel_weight 只看局部）。
3. **absent 探针的停点复用**：认证探针尽量放在"本来就要停"的源位置上（G 方案里 k_abs 从 7 降到 6-6.5 的那部分），在线版 = 扫描顺序里把环点与已检测源的清除合并停。
4. 接受 265.72 收口，把精力转回 Q4（那边 549→350 的空间更大）。

## 六、文件索引

- `_floor_q3_d.py/_f.py/_g.py`：上帝信息 D/F/G 参考解（**均为启发式，不是下界**；
  `_floor_q3_g.py` 的 k_abs 贪心有高估，报告值取 ≈183.7）
- `_floor_q3_fixed.py`：固定 7 停靠布局下的参考解（196.9；按 clear=5s 修正 ≈199.9），**同样不是下界**
- `_study_est.py`：离线估计器命中率研究
- `_diag_clears.py [v8|v10]`：清除尝试插桩
- `framework/src/mathmodel2026b/strategy_v10~v13.py`：四个被否决的方案（保留供复查）
- `_ab_*.txt`：各消融的 30 seeds 原始输出
