# 说明 —— 线上演练测试可行性 & 问题3 现状摸底与优化（存档报告）

> 生成时间：2026-09-11 21:2x（北京时间）
> 对应工作目录：`/home/kfz/zStorage/zProject/mathmodel/`
> 涉及两个仓库：`mathModel2026B/`（建模与策略）、`login-jammers/`（模拟器客户端逆向与自动化）
>
> **本报告只作本地存档，不对外发布、不上传平台。** 见文末"合规与风险"一节。

---

## 0. 一页结论（TL;DR）

| # | 结论 | 证据 |
| --- | --- | --- |
| 1 | **线上平台的正式测试次数已经恢复**：`problem3_remaining_attempts=3`、`problem4_remaining_attempts=3`、`pending_upload_count=0` | `script/preflight.py` 实测登录返回 |
| 2 | **"普通/演练测试"可以完全不依赖官方 Windows 客户端跑通**：练习赛题是客户端本地生成的，服务器只收事后上报的统计值；且当前 `practice_summary_upload_enabled=false`，行为包也不用传 | `script/probe_practice.py` 成功拿到 `practice_ticket_b64` |
| 3 | **但本机没有官方模拟器**（`jammers-simulator.exe` 在整机、Windows 分区、外接介质里都搜不到）。所以线上演练能验证"接口链路"，**不能替代官方 simcore 的物理** | `find` 全盘搜索均无结果 |
| 4 | 已把线上演练全流程实现成脚本并本地跑通（**按指示未上报任何统计**） | `script/run_practice_online.py`，seed=11 跑出 13/13 |
| 5 | **问题3 找到了一个结构性的大改进并已验证**：把"按频道号顺序逐个处理"改成"在线最近邻路线"，再配合更小的逼近半径 → **平均定位清除时间 558.4s → 349.8s（−37.4%）**，200 个随机案例全部清完 | `logs/validate_final.json` |
| 6 | 优化后**覆盖扫描段已经接近理论最优**（同族只差 17–84s），**不建议再投入**；剩余空间在"逼近段"和"每停点 20 频道全扫" | `script/q3_cover_design.py` 数值搜索 |
| 7 | `scan_radius` 还有 ~2.6% 的空间（1125 比 1200 省 114s），但会把覆盖余量从 31m 压到 **0.9m**；结论是**保留 1200**，不建议为 2.6% 冒"漏掉一个源"的风险 | 60 seed 对照 |

---

## 1. 线上平台当前状态（实测）

```
GET  /api/v1/status                       （无需鉴权）
POST /api/v1/auth/login                   （带 device_report）
POST /api/v1/auth/presence / renew
```

实测返回（2026-09-11T12:52Z ≈ 北京时间 20:52）：

```json
{
  "server_time": "2026-09-11T12:52:36.853Z",
  "test_start_deadline": "2026-09-13T09:30:00.000Z",
  "new_tests_enabled": true,
  "practice_summary_upload_enabled": false,
  "minimum_client_version": "1.1.0",
  "latest_client_version": "1.1.0",
  "server_protocol_version": "server-protocol-v1",
  "endpoint_manifest_version": 1
}
```

登录返回：

```json
{
  "team_no": "<队号已隐去>",
  "device_digest": "<64位十六进制，已隐去>",
  "problem3_remaining_attempts": 3,
  "problem4_remaining_attempts": 3,
  "pending_upload_count": 0,
  "test_start_deadline": "2026-09-13T09:30:00.000Z"
}
```

**要点**

- `test_start_deadline = 2026-09-13T09:30:00Z` = **北京时间 2026-09-13 17:30**，与题面一致；官方"建议 15:30 前完成正式测试"。
  → 目前（9-11 晚）距硬截止约 **44 小时**。
- `new_tests_enabled = true`，可以启动新的演练/正式测试。
- 3 次正式机会**看起来已经恢复**（或者此前占用不在当前 device_digest 上）。用 `script/preflight.py` 可随时复查。
- 平台公告原文（节选）："正式测试没有上传完成时，不要退出模拟器！""提交代码时注意隐去参赛队号"。

复现命令：

```bash
cd mathModel2026B
python3 script/preflight.py          # status + robot 端口 + login + presence + renew
```

> ⚠️ 登录会占用该账号的设备会话；如果之后要在自己的模拟器里登录，先
> `python3 <login-jammers>/linux-client/python/jammers_auth.py logout --session-file <…>/jm.session.json`，
> 否则会撞 `account_already_active`(409)。

---

## 2. 官方模拟器缺失这件事

题面附录3/4 要求通过官方 Windows 客户端 `jammers-simulator.exe`（Go 1.27.1 + Wails v3）：
它既是 **GUI**，也是**模拟核心 `internal/simcore`**，同时对外暴露
`http://127.0.0.1:2026` 的 robot API（`robot-protocol-v1`）。

**实际情况**

| 检查项 | 结果 |
| --- | --- |
| 工作区、`login-jammers/` | 无 exe（`.gitignore` 里就写明"客户端二进制不提交"） |
| `/mnt/windows`（本机 Windows 分区全盘） | 无 |
| `/media/*`、`/opt`、`/srv`、`/tmp`、`$HOME` | 无 |
| Wine | 已安装（`/usr/bin/wine`, `wine64`），但没有可执行文件 |

**这意味着什么**

- robot API 由 `internal/portguard` 门控：**端口一直 LISTEN，但没有进行中的测试时新连接会被直接 reset**。
  所以"用 CLI 登录"无论如何都打不开它——必须有官方客户端在跑并在 GUI 里点了"开始测试"。
- 想在本机获得官方物理，只有两条路：
  1. 把 `jammers-simulator.exe` 拿到本机，试 Wine（Wails v3 + WebView2 在 Wine 下不一定能起来，需实测）；
  2. 在别的 Windows 机器上跑官方客户端，用 SSH 反向/正向隧道把它的 `127.0.0.1:2026` 映射到本机，
     本机这边 `python3 script/run.py --mode live` 即可（策略代码同一份）。

---

## 3. 线上"演练/普通测试"：已经证明可以脱离官方客户端

### 3.1 为什么可以

逆向 `login-jammers/analysis/typedump/types_all.txt` 得到的结论：

1. `POST /api/v1/practice-tests/authorize` 返回的**票据里没有任何案例数据**——
   只有队伍/设备/构建身份 + 一个随机 nonce + 有效期。也就是**练习案例是客户端本地生成的**。
2. 服务器收到的只有客户端事后上报的 `POST /api/v1/practice-tests/statistics`
   （清除个数、虚拟时间、检测次数、切频道次数、案例编码、源总数……）。
3. 当前 `/api/v1/status` 显示 `practice_summary_upload_enabled = false`，
   所以连"行为包上传"（`/api/v1/practice-summaries`，需要 Ed25519 签名 + RSA-OAEP 包 DEK）都**不需要**。

于是整条链路是：

```
login ──► authorize(拿票据) ──► 本地跑案例(robot API 挂在 127.0.0.1:2026) ──► statistics(上报)
  服务器只做三件事：发票据、收统计、发公告
```

### 3.2 关键坑：公钥编码

`authorize` 的请求体形状（来自匿名结构体 `types_all.txt:3476`）：

```json
{
  "client_request_id": "<uuid4>",
  "problem_no": 3,
  "client_version": "1.1.0",
  "scenario_schema_version": "scenario-v1",
  "ruleset_version": "rules-v1",
  "package_envelope_version": 1,
  "package_signing_public_key_b64": "<Ed25519 公钥>"
}
```

7 个字段**全部必填且不允许多余字段**——实测：

| 情况 | 服务器返回 |
| --- | --- |
| 字段名写错（snake_case / PascalCase 都不对时） | `400 invalid_request` **"请求字段不正确"** |
| 字段名对、公钥用**标准 base64**（含 `+` `/` `=`） | `400 invalid_request` **"请求内容不正确"** |
| **公钥用 base64url 且去掉 `=` 填充**（32 字节原始 Ed25519 公钥） | **`201` → `{"practice_ticket_b64": "...", "server_time": "..."}`** |
| 多传 `team_no` / `device_digest` / `build_id` 等 | `400` "请求字段不正确" |
| 少传 `scenario_schema_version` 等 | `400` "请求内容不正确" |

> 这条"标准 base64 会被拒、必须 base64url 无填充"的差异，是最容易卡住的一步。
> 探针脚本：`script/probe_practice.py`（正确形状）、`script/probe_practice_variants.py`（12 个变体对照表）。

### 3.3 票据格式（实测解出）

```
offset 0   8B   magic  "JMBTKT1\0"
offset 8   2B   version (big-endian, =1)
offset 10  4B   json_len (big-endian)
offset 14  N B  JSON claims
offset 14+N 64B Ed25519 签名
```

claims 示例：

```json
{
  "ticket_type": "practice_ticket_v1",
  "ticket_version": 1,
  "team_no": "<队号已隐去>",
  "problem_no": 3,
  "device_digest": "<已隐去>",
  "client_version": "1.1.0",
  "build_id": "b625dc14aa816fdaf35b6f1984dcf7b2",
  "build_manifest_sha256": "2a6739bb…",
  "authorization_nonce_b64": "NUMYra04qNVtNM-7hCvj0A",
  "package_signing_public_key_b64": "53MG9Qd1…",
  "issued_at": "2026-09-11T13:06:29.997Z",
  "start_before": "2026-09-13T09:30:00.000Z",
  "signing_key_id": "ticket-2026-a"
}
```

**尚未完全逆向的一处**：票据尾部的 64 字节签名，用构建清单里的 `ticket-2026-a/b`
公钥对「JSON 段」「magic+头+JSON」两种消息都验签失败，说明它用了域分隔前缀或不同的
签名布局。**这不影响使用**（票据是给服务器/客户端校验用的，我们只把它当作
`practice_ticket_sha256` 的输入），但如果以后要独立实现正式测试流程，这一点需要补上。

### 3.4 已实现的全流程脚本

`script/run_practice_online.py`：

```bash
# 只 authorize + 本地跑，不上报（默认）
python3 script/run_practice_online.py --seed 11

# 完整走到 statistics
python3 script/run_practice_online.py --seed 11 --submit
```

它做的事：
1. 登录 → 2. 生成 Ed25519 密钥对 → 3. `authorize` 拿票据并解码 →
4. 起 `MockSimulator` 并**把它挂在真实的 `127.0.0.1:2026` 端口**上 →
5. 用 `run.py` 的 `--mode live` **同一条代码路径**跑策略（事件流格式与线上一致）→
6. 组装 `PracticeStatisticsCommand` 字段 → 7. （可选）上报。

本地实跑记录（`logs/practice_online_20260911T130828Z.json`）：

```
status  new_tests_enabled=True deadline=2026-09-13T09:30:00.000Z practice_summary_upload_enabled=False
login   ok team_no=<队号已隐去> p3=3 p4=3
authorize ok (0.25s) nonce=CKGznfZVutZGfmuIBINWrw start_before=2026-09-13T09:30:00.000Z
robot API (本地 simcore) -> http://127.0.0.1:2026
run     20260911T130827Z_s11_online-practice_5571ac status=ok cleared=13/13 virtual_s=8430.5 wall_s=0.29
statistics  未上报（加 --submit 才会调用）
```

### 3.5 **重要限定：这不是官方演练成绩**

第 4 步的模拟核心是我们自己的 `framework/src/mathmodel2026b/mock/world.py`，
**不是**官方 `internal/simcore`。所以：

- 这条链路**能证明**：登录 / 票据 / 机器人接口形状 / 统计上报字段全部正确 —— 即"线上接口链路通"；
- 它**不能证明**：官方物理下能清几个、平均定位清除时间是多少；
- **正式测试另当别论**：`formal-tests/prepare` 下发的 `sealed_case_package` 是**服务器加密的**，
  解锁信封里的 DEK 用客户端生成的 RSA 公钥包裹。要在正式测试里拿到真题案例，
  必须有官方客户端（或用 Python 复刻 RSA-OAEP 解包 + 案例包解码——属于"独立大工程"，且不建议）。

→ **论文《表1 问题3/4 正式测试结果》里的数字，只能用官方客户端跑出来的。**

---

## 4. 问题3 现状摸底（"先摸清再优化"）

### 4.1 时间到底花在哪（旧默认参数，24 个 seed，全部清完）

虚拟时间构成（题面口径：移动 5m/s + 切换 1s + 检测 5s + 光学定位 3s + 清除 2s）：

| 项目 | 中位数 | 占比 |
| --- | --- | --- |
| 虚拟总时间 | **7039 s** | 100% |
| ├ 移动（里程/5） | 6087 s | **86.5%** |
| └ 检测 5s×n + 切换 1s×n + 清除 5s×n | 952 s | 13.5% |
| 路程 | **30436 m** | — |
| 平均定位清除时间 | 546.7 s | — |

**结论一：这是一个纯路程问题。时间几乎就是里程的函数（1 m = 0.2 s）。**

按决策类别拆路程（`script/q3_time_audit.py`）：

| 决策类别 | 路程(m) | 占比 | 事件数 |
| --- | --- | --- | --- |
| `measure@refine`（逼近取读数） | 14653 | **48.1%** | 10 |
| `clear@localized`（走到清除点） | 7562 | 24.8% | 12 |
| `measure@cover-scan`（覆盖扫描） | 7200 | 23.7% | 140 |
| `measure@refine-fallback` | 718 | 2.4% | 1 |

**结论二：73% 的里程花在"定位某个源"这件事上；覆盖扫描只占 24%（且它是固定成本）。**

### 4.2 覆盖扫描之后，源已经被定得多准了？（30 seed / 389 个被探测到的频道）

| 读数个数 | 频道数 | 可行域直径中位 | 最小包围圆半径中位 | 已经 ≤20m（可直接清） |
| --- | --- | --- | --- | --- |
| 1 | 65 | 648 m | 324 m | 0.0% |
| 2 | 140 | 47 m | 24 m | 0.7% |
| 3 | 105 | 39 m | 20 m | 51.4% |
| 4 | 64 | 33 m | 16 m | 87.5% |
| 5 | 13 | 26 m | 13 m | 100% |
| ≥6 | 2 | 19–35 m | 9–18 m | 100% |

汇总：包围圆半径 P50 = **21 m**；**≤20m 的只有 32.4%**，但 **≤60m 的已有 82.3%**。

**结论三：扫描结束时绝大多数源已经"差一点点"。真正贵的不是"再测几次"，而是"走过去"。**
一次检测只花 5s ＝ 25m 的走路时间——**检测非常便宜，走路非常贵**，
所以任何"为了省 25m 检测而多走 300m"的取舍都是错的。

### 4.3 同族策略的距离下界

对每个案例：覆盖扫描 7200 m（起点即圆心，0→6 个顶点各 1200 m，已经是该 7 点集的最短巡游），
之后**自由排序**访问全部源（源到最近"能收到它的扫描点"的距离中位 497 m；用最近邻+2-opt 求访问序）：

| 项 | 中位 |
| --- | --- |
| 覆盖扫描 | 7200 m |
| 扫描后访问全部源的最优路径 | 8977 m |
| **同族距离下界** | **16177 m** |
| 旧默认实测 | 30666 m |
| 可压缩 | **≈14489 m（49%）** |

**结论四：旧实现有一半的里程是可压缩的，而且压缩空间几乎全在"源的访问顺序 + 逼近方式"，不在覆盖扫描。**

### 4.4 覆盖扫描本身还有多大空间？（数值搜索，负结果）

把"每个停点要扫 20 个频道"的真实代价算进去（20 × 6s = 120 s/停点），
在"正 n 边形巡游 + 边上加点 + 原点免费起点"这一族里搜索
`min(路程/5 + 120×停点数)`：

| 设计 | 停点 | 路程 | 移动 | 检测 | 合计 |
| --- | --- | --- | --- | --- | --- |
| **当前：六边形 r=1200** | 7 | 7200 | 1440 s | 840 s | **2280 s** |
| 六边形 r=1130 | 7 | 6780 | 1356 s | 840 s | 2196 s（−84 s） |
| 六边形 r=1150（余量 ≥10 m） | 7 | 6900 | 1380 s | 840 s | 2220 s（−60 s） |
| 七边形 r=1050（余量 ≥30 m） | 8 | 6990 | 1398 s | 960 s | 2263 s（−17 s） |

**结论五：覆盖扫描设计基本已经榨干（同余量下只差 17 s）。不要再在这里投时间。**

> 顺带记一个方法论陷阱：如果**只**看路程，11 边形 r=870（12 个停点）只要 5772 m，
> 看起来能省 1400 m；但多出的 5 个停点要各自扫 20 个频道，检测时间反而多 600 s。
> **"路程最短"不等于"时间最短"**，本问题里 1 s = 5 m。

**顺带量了 `scan_radius`（60 seed，优化后的策略）：**

| scan_radius | 覆盖半径 | 虚拟时间中位 | 平均定位清除时间中位 | 未全清除 |
| --- | --- | --- | --- | --- |
| 1125 | 999.1 m（余量 **0.9 m**） | 4291.2 | **331.9** | 0 |
| 1150 | 988.5 m（余量 11.5 m） | 4350.4 | 343.2 | 0 |
| 1200（当前默认） | 968.9 m（余量 31 m） | 4404.6 | 344.0 | 0 |
| 1250 | 951.5 m | 4502.5 | 353.1 | 0 |
| 1300 | 936.5 m | 4608.4 | 362.5 | 0 |

1125 比 1200 省约 2.6%，但覆盖余量只剩 0.9 m。**结论：保留 1200。**
"确保所有干扰源被清除"是硬前提，为 2.6% 去冒漏掉一个源的风险不划算。

---

## 5. 问题3 优化：做了什么，效果如何

### 5.1 两处结构性改动

**改动 A：访问顺序从"按频道号"改成"在线最近邻路线"**

原 `_finish_pending` 是 `for channel in sorted(state.uncleared_detected)`——
等于让机器狗在 1800 m 的圆域里**按频道号随机横跳**。
而"访问所有源"本来就是一个 TSP。改成：每处理完一个源，就用
"当前位置 + 最新估计点"重新挑最近的下一个（`_nearest_pending`）。

**这一条就是绝大部分收益。**

**改动 B：逼近半径 200/400/700 → 30/60/120/250，并给打分加入"离源多近"这一项**

原打分只有「角度分离 − 路程」：

```python
score = sep_weight * min(sep, 90) / 90 - travel_weight * travel
```

**完全没有"离源多近"这一项**，而定位完成后仍要走到源那里，那一段是大头。
新增 `approach_weight × (候选点到可行域的平均距离)`（`_region_samples` 在可行域内撒点估计）。

**改动 C（小）：`skip_localized_probes`**

一个频道只对应一个源（附录1-1），所以可行域最小包围圆一旦 ≤ 20 m，
再测这个频道不会有任何新信息。扫描阶段直接跳过，省下 1s 切换 + 5s 检测。

### 5.2 效果（200 个随机案例，全部清完）

| 指标 | 旧默认 | 新默认 | 变化 |
| --- | --- | --- | --- |
| 未全清除的案例 | 0 / 200 | 0 / 200 | — |
| **平均定位清除时间（中位）** | **558.4 s** | **349.8 s** | **−37.4%** |
| 平均定位清除时间（均值） | 563.3 s | 351.0 s | −37.7% |
| 平均定位清除时间范围 | [421.4, 791.6] | [260.1, 456.7] | — |
| 虚拟总时间（中位） | 7098.1 s | 4434.5 s | −37.5% |
| 路程（中位） | 30666 m | 17609 m | −42.6% |
| 墙钟（200 例最长） | — | < 1 s | 远低于 20 min 上限 |

优化后的构成（24 seed）：

| 项目 | 中位 | 占比 |
| --- | --- | --- |
| 虚拟总时间 | 4339 s | 100% |
| ├ 移动 | 3407 s | 78.5% |
| └ 非移动（检测/切换/清除） | 932 s | 21.5% |
| `measure@cover-scan` | 7200 m | 42.3% |
| `measure@refine` | 7005 m | 41.1% |
| `clear@localized` | 3126 m | 18.4% |

路程 17609 m vs 同族下界 16177 m → **只差 8.8%**，说明这条策略族的路线已经基本走到头。

### 5.3 逐步归因（每步单独跑的对照）

40–60 seed 对照（中位虚拟时间）：

| 配置 | bad | 虚拟时间 | 路程 | 平均定位清除时间 |
| --- | --- | --- | --- | --- |
| 旧默认（channel, 200/400/700, aw=0） | 0 | 7309.7 | 31721 | 554.6 |
| + nearest 顺序 | 0 | 4799.1 | 19212 | 369.3 |
| + 逼近半径 (30,60,120,250) | 0 | 4485.1 | 17647 | 345.0 |
| + approach_weight=1/500 | 0 | 4423.0 | 17263 | 343.4 |
| + skip_localized_probes | 0 | 4404.6 | 17545 | 344.0 |

另外单独量过：单纯把 `travel_weight` 从 1/1200 调大到 1/100 只能带来约 5% 收益，
而且会**开始出现清不完的案例**（bad=4~8/20）——这就是"不断切换指标贪心"撞墙的地方。

---

## 6. 代码改动清单

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `framework/src/mathmodel2026b/strategy.py` | **改** | 新增参数 `approach_weight` / `visit_order` / `skip_localized_probes`；新默认 `approach_radii=(30,60,120,250)`；新增 `_nearest_pending` / `_is_localized` / `_region_samples`；`_finish_pending` 改为在线最近邻 |
| `script/run_practice_online.py` | 新增 | 线上演练全流程（authorize→本地跑→可选 statistics） |
| `script/probe_practice.py` | 新增 | 练习 authorize 单次探针（正确请求形状） |
| `script/probe_practice_variants.py` | 新增 | 12 个请求体变体对照，定位"字段/内容"两类报错 |
| `script/q3_time_audit.py` | 新增 | 按决策类别拆虚拟时间与路程 |
| `script/q3_diagnose_scan.py` | 新增 | 覆盖扫描后各频道可行域质量统计 |
| `logs/validate_final.json` | 新增 | 200 seed 旧/新默认完整指标 |
| `logs/validate_100.json` | 新增 | 100 seed 对照 |
| `logs/practice_probe.json` | 新增 | 练习票据与 claims |
| `logs/practice_online_20260911T130828Z.json` | 新增 | 线上演练本地跑的一次完整记录 |

测试：`cd framework && python3 -m pytest -q` → **55 passed**。

---

## 7. 下一步路线图（按预期收益排序）

| 优先级 | 方向 | 预期收益 | 备注 |
| --- | --- | --- | --- |
| ★★★ | **逼近段（7005 m / 41%）**：把"一步贪心打分"换成**有 cost-to-go 的序贯决策**。状态 = (可行域多边形, 当前位置)，动作 = 下一个检测点，代价 = 路程/5 + 6 s，终止 = 包围圆 ≤ 20 m。可用小宽度 beam search 近似。理论上"先沿方位线接近到 ~0.3d、再横向 ~0.15d"是两点测向的最优解 | 1–2 km（200–400 s） | 这就是"复杂决策树"真正该用的地方 |
| ★★ | **每停点 20 频道全扫（840 s / 19%）**：现在只跳过"已 ≤20 m"的频道；可以考虑按"该频道是否还可能在本停点覆盖范围内有源"做更激进的剪枝 | 100–200 s | 需要保守的正确性论证 |
| ★ | **覆盖扫描（7200 m + 840 s）**：已接近最优 | ≤ 84 s | **不建议再投入** |
| ★★ | **问题4（定向源）**：定向源覆盖角只有 ±90°，`no_signal` 不再等价于"太远"，可行域要加**非凸**约束，策略需分支/多假设 | 目前直接跑会明显掉清除率 | 尚未开工 |
| ★★★ | **拿到官方模拟器**：Wine 跑 exe，或用 SSH 隧道把别的 Windows 机的 `127.0.0.1:2026` 映射过来 | 决定正式测试能不能做 | 见第 2 节 |

---

## 8. 合规与风险（请务必先看这一节）

1. **本次没有向平台提交任何演练统计。** `run_practice_online.py` 默认 `--submit` 关闭；
   实测那次也只停在本地上报前的最后一步，`logs/practice_online_*.json` 里
   `statistics_response` 字段不存在。
2. **不要把这个 Python 客户端当作"正式测试的替代品"。**
   正式测试的案例包由服务器加密下发，官方客户端的解锁与物理不可替代；
   而且用自研 simcore 的统计去占正式测试名额，属于对平台的误导，风险不可控。
3. **本文档只作本地存档**，不提交、不分享、不在任何公开渠道讨论赛题解法（平台公告明确禁止赛期交流）。
4. 提交论文/支撑材料时注意隐去队号（平台公告要求）。
5. 演练测试不限次数但**占用账号设备会话**：本机 CLI 登录后，官方客户端登录会撞 `account_already_active`。
   切换前先 `logout`。
